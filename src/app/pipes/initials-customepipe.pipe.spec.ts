import { InitialsCustomepipePipe } from './initials-customepipe.pipe';

describe('InitialsCustomepipePipe', () => {
  it('create an instance', () => {
    const pipe = new InitialsCustomepipePipe();
    expect(pipe).toBeTruthy();
  });
});
