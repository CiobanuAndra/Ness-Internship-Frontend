export enum RequireAttentionTable {
    name = 'name',
    surname = 'surname',
    email = 'email',
}

export enum AwaitConfirmationTable {
    name = 'name',
    surname = 'surname',
    email = 'email',
    options = 'options',
}