export interface Course {
    title: string,
    task: string,
    link: string,
    fileType: string,
    duration: number,
    rewardQuantity: number,
    points: number,
    dateCreated: Date,
    lastEdited: Date,
    editedBy:string,
}